{
  "name": "first-up",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "build": "node scripts/build.mjs",
    "preview": "python3 -m http.server 4173 --directory dist"
  }
}
